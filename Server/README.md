To setup the server, point the cmd console to this folder and execute the following commands:

npm install ws
npm install uniqid

to start the server then, point to the /src folder and execute the following command:

node server
